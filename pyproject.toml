[project]
name = "yolo-image-project"
version = "0.1.0"
description = "YOLO image inference"
readme = "README.md"
requires-python = ">=3.10,<3.12"

dependencies = [
    "torch",
    "torchvision",
    "ultralytics",
    
]
